model_list:
  # brain (DeepSeek V3.1 via Synthetic)
  # Hybrid reasoning on US/EU hardware (no Chinese servers)
  - model_name: brain
    litellm_params:
      model: synthetic/hf:deepseek-ai/DeepSeek-V3.2
      api_key: os.environ/SYNTHETIC_API_KEY

  # brain-opus (Claude Opus 4.6 via OpenRouter)
  # Top-tier reasoning at $15/$75 per M tokens
  - model_name: brain-opus
    litellm_params:
      model: openrouter/anthropic/claude-opus-4-6
      api_key: os.environ/OPENROUTER_API_KEY

  # brain-gemini (Gemini 2.5 Flash with thinking)
  # Fast reasoning, 1M context at $0.15/$0.60 per M tokens
  - model_name: brain-gemini
    litellm_params:
      model: gemini/gemini-2.5-flash
      api_key: os.environ/GEMINI_API_KEY

  # brain-kimi (Kimi K2.5 via Synthetic)
  # Multimodal agentic reasoning on US/EU hardware (no Chinese servers)
  - model_name: brain-kimi
    litellm_params:
      model: synthetic/hf:moonshotai/Kimi-K2.5
      api_key: os.environ/SYNTHETIC_API_KEY

  # coder (Gemini 2.5 Flash)
  # Fast, capable coder at $0.15/$0.60 per M tokens
  - model_name: coder
    litellm_params:
      model: gemini/gemini-2.5-flash
      api_key: os.environ/GEMINI_API_KEY

  # heartbeat (Gemini 2.5 Flash-Lite)
  # Ultra-cheap at $0.075/$0.30 per M tokens
  - model_name: heartbeat
    litellm_params:
      model: gemini/gemini-2.5-flash-lite
      api_key: os.environ/GEMINI_API_KEY

  # perplexity (Perplexity Sonar via OpenRouter)
  # Web search grounded answers at ~$1/M tokens
  - model_name: perplexity
    litellm_params:
      model: openrouter/perplexity/sonar
      api_key: os.environ/OPENROUTER_API_KEY

  # fallback brain (Gemini 2.5 Flash)
  - model_name: brain-fallback
    litellm_params:
      model: gemini/gemini-2.5-flash
      api_key: os.environ/GEMINI_API_KEY

litellm_settings:
  drop_params: true
  fallbacks:
    - brain: ["brain-fallback"]
  num_retries: 2

router_settings:
  routing_strategy: simple-shuffle
  num_retries: 2
  timeout: 120

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY

